# GitHub Account Consolidation — Status

## Identity

- Malik Hashir = `iamhashir` = `ihashirr`
- **Canonical professional account:** `iamhashir`
- **Legacy historical account:** `ihashirr`
- **Collaborative organization:** `minapong`

The goal is one recruiter-facing identity without destroying historical commits, PRs, or collaboration evidence attached to the older username.

---

## Completed

### Repository ownership

Transferred from `ihashirr` to `iamhashir`:

- ✅ `IRC`
- ✅ `mess-manager` — remains private
- ✅ `secret-hitler`
- ✅ `Code-Smith`

### Post-transfer repairs

- ✅ `42curcus/.gitmodules` now points `rank5/ft_irc` to `https://github.com/iamhashir/IRC.git`
- ✅ `IRC` README rebuilt from the actual non-blocking server implementation
- ✅ `secret-hitler` README rebuilt from its Convex game engine/state machine/bot implementation
- ✅ `Code-Smith` README rebuilt from its actual prototype architecture and export flow
- ✅ master repository ranking refreshed in `REPOSITORIES.md`

### Deliberate non-migrations

Do **not** transfer old standalone 42 repositories when the same work is already represented in `iamhashir/42curcus`:

- `cube` / Cub3D
- `minitalk`
- `pushing-swap`
- `printf-42`
- `42-libft`
- exam repositories

This keeps the main account from becoming another duplicate-heavy archive.

---

## Keep in `minapong`

`minapong/ft_transcendence` stays in the organization.

Reason: its organization ownership is useful evidence that MINA GAMES was collaborative work. The main profile should link/pin it directly rather than copying it into a personal repository.

---

## Optional migrations — blocked on cleanup

### `Qrcode_nisrine`

Before migration:

- review client/company information intended to remain public
- verify brand assets and business identifiers are safe to expose
- then transfer only if it adds a useful distinct public signal

### `Arduino`

Before migration:

- remove hardcoded Wi-Fi SSID/password/config from tracked source
- replace with an ignored local configuration/header or documented placeholders
- rotate credentials if they were ever real/reused

After cleanup, this is worthwhile because the project adds genuine embedded/IoT breadth.

### `Movie-Recommendation-System`

Optional. Transfer only after adding a concise methodology/results README; it is supporting data/ML evidence rather than a flagship.

---

## Main-account cleanup still manual

The connector cannot currently perform these account/repository settings operations, so they remain GitHub UI tasks.

### Recommended six pins

1. `minapong/ft_transcendence`
2. `iamhashir/real_estate_management`
3. `iamhashir/IRC`
4. `iamhashir/secret-hitler`
5. `iamhashir/Customer_service_agent`
6. `iamhashir/Audify`

If the organization repository is unavailable in the pin selector, use `iamhashir/42curcus` as the replacement.

### High-priority archive/unfeature group

- `github-slideshow`
- `story`
- empty `pushing-swap`
- `hassan_philo`
- `hasan_philo`
- `Clone_1`
- `Clone_task_3`
- `2nd_clone`
- `React-Clone`
- `Color-Switch-Replica`
- `react_tail`
- exam-practice repositories
- redundant tourism variants after confirming `tourism_app` as canonical
- `hashir-portfolio` after confirming `cv_portfolio` as canonical

Archive first; delete only when there is no historical value.

---

## Legacy account end state

`ihashirr` should remain alive for historical attribution but stop receiving new work.

Recommended legacy profile message:

```text
Malik Hashir

This is a legacy GitHub identity retained for historical contribution attribution.
Active profile: @iamhashir
```

All new repositories, commits, PRs, CV links, portfolio links and job applications should use `iamhashir`.

---

## Definition of done

The consolidation is effectively complete when:

- one canonical public profile is used everywhere
- the six strongest repositories are pinned
- obvious duplicate/tutorial repositories are archived or unfeatured
- `ihashirr` redirects visitors to `iamhashir`
- all new engineering activity happens under `iamhashir`

The ownership migration itself is now complete for the four priority repositories.
